---
export const prerender = true;
---

<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex, nofollow" />
  <title>Lichtblick — Yoga & Meditation in Konz</title>
  <meta name="description" content="Dein Raum für innere Ruhe. Yoga, Meditation und Achtsamkeit in Konz bei Trier. Hatha, Vinyasa, Yin Yoga und mehr." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary: #7C9885;
      --primary-dark: #6a8573;
      --secondary: #F7F3EE;
      --accent: #D4A574;
      --accent-light: #e4c4a0;
      --bg: #FDFBF8;
      --text: #2D3436;
      --text-light: #636e72;
      --white: #ffffff;
      --shadow-soft: 0 2px 16px rgba(0, 0, 0, 0.06);
      --shadow-medium: 0 4px 24px rgba(0, 0, 0, 0.08);
      --radius: 16px;
      --radius-sm: 10px;
      --radius-full: 9999px;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      color: var(--text);
      background-color: var(--bg);
      line-height: 1.7;
      font-size: 16px;
      -webkit-font-smoothing: antialiased;
    }

    /* ===================== NAV ===================== */
    .nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      background: rgba(253, 251, 248, 0.85);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-bottom: 1px solid rgba(124, 152, 133, 0.1);
      transition: box-shadow 0.3s ease;
    }

    .nav.scrolled {
      box-shadow: var(--shadow-soft);
    }

    .nav-inner {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
      height: 72px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .nav-logo {
      font-size: 1.35rem;
      font-weight: 600;
      color: var(--primary);
      text-decoration: none;
      letter-spacing: -0.02em;
    }

    .nav-logo span {
      color: var(--accent);
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 32px;
      list-style: none;
    }

    .nav-links a {
      text-decoration: none;
      color: var(--text-light);
      font-size: 0.9rem;
      font-weight: 500;
      transition: color 0.2s ease;
      position: relative;
    }

    .nav-links a:hover {
      color: var(--primary);
    }

    .nav-links .nav-cta {
      background: var(--primary);
      color: var(--white);
      padding: 10px 22px;
      border-radius: var(--radius-full);
      font-weight: 500;
      transition: background 0.2s ease, transform 0.2s ease;
    }

    .nav-links .nav-cta:hover {
      background: var(--primary-dark);
      color: var(--white);
      transform: translateY(-1px);
    }

    .nav-toggle {
      display: none;
      background: none;
      border: none;
      cursor: pointer;
      padding: 8px;
    }

    .nav-toggle span {
      display: block;
      width: 22px;
      height: 2px;
      background: var(--text);
      margin: 5px 0;
      border-radius: 2px;
      transition: all 0.3s ease;
    }

    /* ===================== HERO ===================== */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      position: relative;
      overflow: hidden;
      padding-top: 72px;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
    }

    .hero-bg img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center 30%;
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        135deg,
        rgba(253, 251, 248, 0.92) 0%,
        rgba(253, 251, 248, 0.78) 50%,
        rgba(253, 251, 248, 0.55) 100%
      );
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 1200px;
      margin: 0 auto;
      padding: 80px 24px 60px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 64px;
      align-items: center;
    }

    .hero-text {
      max-width: 540px;
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--white);
      border: 1px solid rgba(124, 152, 133, 0.2);
      padding: 6px 16px;
      border-radius: var(--radius-full);
      font-size: 0.8rem;
      font-weight: 500;
      color: var(--primary);
      margin-bottom: 28px;
      box-shadow: var(--shadow-soft);
    }

    .hero-badge::before {
      content: '';
      width: 6px;
      height: 6px;
      background: var(--primary);
      border-radius: 50%;
      animation: pulse-dot 2s ease-in-out infinite;
    }

    @keyframes pulse-dot {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.4; }
    }

    .hero h1 {
      font-size: clamp(2.8rem, 5vw, 4rem);
      font-weight: 700;
      line-height: 1.1;
      letter-spacing: -0.03em;
      color: var(--text);
      margin-bottom: 20px;
    }

    .hero h1 em {
      font-style: normal;
      color: var(--primary);
    }

    .hero-subtitle {
      font-size: 1.1rem;
      color: var(--text-light);
      line-height: 1.7;
      margin-bottom: 36px;
      max-width: 440px;
    }

    .hero-actions {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      margin-bottom: 56px;
    }

    .btn-primary {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--primary);
      color: var(--white);
      padding: 15px 32px;
      border-radius: var(--radius-full);
      text-decoration: none;
      font-weight: 600;
      font-size: 0.95rem;
      border: none;
      cursor: pointer;
      transition: all 0.25s ease;
      box-shadow: 0 4px 16px rgba(124, 152, 133, 0.3);
    }

    .btn-primary:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
      box-shadow: 0 6px 24px rgba(124, 152, 133, 0.4);
    }

    .btn-ghost {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: transparent;
      color: var(--text);
      padding: 15px 32px;
      border-radius: var(--radius-full);
      text-decoration: none;
      font-weight: 500;
      font-size: 0.95rem;
      border: 1.5px solid rgba(45, 52, 54, 0.2);
      cursor: pointer;
      transition: all 0.25s ease;
    }

    .btn-ghost:hover {
      border-color: var(--primary);
      color: var(--primary);
      background: rgba(124, 152, 133, 0.05);
    }

    .hero-trust {
      display: flex;
      gap: 40px;
      flex-wrap: wrap;
    }

    .trust-item {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .trust-value {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--text);
    }

    .trust-label {
      font-size: 0.78rem;
      color: var(--text-light);
      font-weight: 400;
    }

    .hero-image-card {
      position: relative;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: var(--shadow-medium);
      aspect-ratio: 3 / 4;
      max-height: 520px;
      justify-self: center;
    }

    .hero-image-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .hero-image-float {
      position: absolute;
      bottom: 24px;
      left: 24px;
      right: 24px;
      background: rgba(255, 255, 255, 0.92);
      backdrop-filter: blur(12px);
      border-radius: var(--radius);
      padding: 16px 20px;
      display: flex;
      align-items: center;
      gap: 14px;
    }

    .hero-image-float-icon {
      width: 40px;
      height: 40px;
      background: var(--primary);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--white);
      font-size: 1.1rem;
      flex-shrink: 0;
    }

    .hero-image-float-text strong {
      display: block;
      font-size: 0.85rem;
      font-weight: 600;
      color: var(--text);
    }

    .hero-image-float-text span {
      font-size: 0.78rem;
      color: var(--text-light);
    }

    /* ===================== SECTION BASE ===================== */
    .section {
      padding: 100px 24px;
    }

    .section-inner {
      max-width: 1200px;
      margin: 0 auto;
    }

    .section-header {
      text-align: center;
      max-width: 560px;
      margin: 0 auto 56px;
    }

    .section-tag {
      display: inline-block;
      font-size: 0.78rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--primary);
      margin-bottom: 12px;
    }

    .section-title {
      font-size: clamp(1.8rem, 3.5vw, 2.4rem);
      font-weight: 700;
      line-height: 1.2;
      letter-spacing: -0.02em;
      margin-bottom: 16px;
    }

    .section-desc {
      font-size: 1rem;
      color: var(--text-light);
      line-height: 1.7;
    }

    /* ===================== KURSE ===================== */
    .kurse {
      background: var(--secondary);
    }

    .kurse-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 24px;
    }

    .kurs-card {
      background: var(--white);
      border-radius: var(--radius);
      padding: 32px 28px;
      box-shadow: var(--shadow-soft);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .kurs-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-medium);
    }

    .kurs-icon {
      width: 52px;
      height: 52px;
      background: linear-gradient(135deg, var(--secondary), rgba(124, 152, 133, 0.12));
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin-bottom: 20px;
    }

    .kurs-card h3 {
      font-size: 1.15rem;
      font-weight: 600;
      margin-bottom: 10px;
      letter-spacing: -0.01em;
    }

    .kurs-card p {
      font-size: 0.88rem;
      color: var(--text-light);
      line-height: 1.6;
      margin-bottom: 20px;
    }

    .kurs-meta {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .kurs-meta-item {
      display: flex;
      align-items: center;
      gap: 8px;
      font-size: 0.8rem;
      color: var(--text-light);
    }

    .kurs-meta-icon {
      width: 16px;
      height: 16px;
      flex-shrink: 0;
      opacity: 0.6;
    }

    .kurs-level {
      display: inline-block;
      margin-top: 16px;
      padding: 4px 12px;
      border-radius: var(--radius-full);
      font-size: 0.72rem;
      font-weight: 600;
      letter-spacing: 0.03em;
    }

    .level-beginner {
      background: rgba(124, 152, 133, 0.12);
      color: var(--primary);
    }

    .level-all {
      background: rgba(212, 165, 116, 0.15);
      color: var(--accent);
    }

    .level-advanced {
      background: rgba(45, 52, 54, 0.08);
      color: var(--text-light);
    }

    /* ===================== KONTAKT ===================== */
    .kontakt {
      background: var(--bg);
    }

    .kontakt-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 64px;
      align-items: start;
    }

    .kontakt-info {
      padding-top: 8px;
    }

    .kontakt-info h3 {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 8px;
      letter-spacing: -0.02em;
    }

    .kontakt-info > p {
      color: var(--text-light);
      font-size: 0.95rem;
      margin-bottom: 32px;
      line-height: 1.7;
    }

    .kontakt-details {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-bottom: 36px;
    }

    .kontakt-detail {
      display: flex;
      gap: 16px;
      align-items: flex-start;
    }

    .kontakt-detail-icon {
      width: 44px;
      height: 44px;
      background: var(--secondary);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .kontakt-detail-icon svg {
      width: 20px;
      height: 20px;
      color: var(--primary);
    }

    .kontakt-detail-text strong {
      display: block;
      font-size: 0.88rem;
      font-weight: 600;
      margin-bottom: 2px;
    }

    .kontakt-detail-text span,
    .kontakt-detail-text a {
      font-size: 0.85rem;
      color: var(--text-light);
      text-decoration: none;
      line-height: 1.6;
    }

    .kontakt-detail-text a:hover {
      color: var(--primary);
    }

    .kontakt-hours {
      background: var(--secondary);
      border-radius: var(--radius);
      padding: 24px;
    }

    .kontakt-hours h4 {
      font-size: 0.9rem;
      font-weight: 600;
      margin-bottom: 12px;
    }

    .kontakt-hours-row {
      display: flex;
      justify-content: space-between;
      font-size: 0.85rem;
      padding: 6px 0;
      border-bottom: 1px solid rgba(124, 152, 133, 0.08);
    }

    .kontakt-hours-row:last-child {
      border-bottom: none;
    }

    .kontakt-hours-day {
      color: var(--text-light);
    }

    .kontakt-hours-time {
      font-weight: 500;
    }

    /* FORM */
    .kontakt-form {
      background: var(--white);
      border-radius: 20px;
      padding: 40px;
      box-shadow: var(--shadow-medium);
    }

    .kontakt-form h3 {
      font-size: 1.2rem;
      font-weight: 600;
      margin-bottom: 6px;
    }

    .kontakt-form > p {
      font-size: 0.85rem;
      color: var(--text-light);
      margin-bottom: 28px;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-group label {
      display: block;
      font-size: 0.82rem;
      font-weight: 500;
      margin-bottom: 6px;
      color: var(--text);
    }

    .form-group input,
    .form-group select,
    .form-group textarea {
      width: 100%;
      padding: 12px 16px;
      border: 1.5px solid rgba(45, 52, 54, 0.12);
      border-radius: var(--radius-sm);
      font-family: 'DM Sans', sans-serif;
      font-size: 0.9rem;
      color: var(--text);
      background: var(--bg);
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
      outline: none;
    }

    .form-group input:focus,
    .form-group select:focus,
    .form-group textarea:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(124, 152, 133, 0.12);
    }

    .form-group input::placeholder,
    .form-group textarea::placeholder {
      color: #b2bec3;
    }

    .form-group textarea {
      resize: vertical;
      min-height: 100px;
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }

    .form-submit {
      width: 100%;
      padding: 15px;
      background: var(--primary);
      color: var(--white);
      border: none;
      border-radius: var(--radius-sm);
      font-family: 'DM Sans', sans-serif;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.25s ease;
      margin-top: 4px;
    }

    .form-submit:hover {
      background: var(--primary-dark);
      box-shadow: 0 4px 16px rgba(124, 152, 133, 0.3);
    }

    .form-hint {
      font-size: 0.75rem;
      color: var(--text-light);
      margin-top: 16px;
      text-align: center;
    }

    /* ===================== FOOTER ===================== */
    .footer {
      background: var(--text);
      color: rgba(255, 255, 255, 0.7);
      padding: 48px 24px 32px;
    }

    .footer-inner {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 16px;
    }

    .footer-logo {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--white);
    }

    .footer-logo span {
      color: var(--accent);
    }

    .footer-links {
      display: flex;
      gap: 24px;
      list-style: none;
    }

    .footer-links a {
      color: rgba(255, 255, 255, 0.5);
      text-decoration: none;
      font-size: 0.82rem;
      transition: color 0.2s ease;
    }

    .footer-links a:hover {
      color: var(--white);
    }

    .footer-copy {
      font-size: 0.78rem;
      color: rgba(255, 255, 255, 0.35);
      width: 100%;
      text-align: center;
      margin-top: 24px;
      padding-top: 24px;
      border-top: 1px solid rgba(255, 255, 255, 0.08);
    }

    /* ===================== MOBILE ===================== */
    @media (max-width: 1024px) {
      .kurse-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 768px) {
      .nav-links {
        display: none;
        position: fixed;
        top: 72px;
        left: 0;
        right: 0;
        bottom: 0;
        background: var(--bg);
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 28px;
        padding: 40px;
      }

      .nav-links.open {
        display: flex;
      }

      .nav-links a {
        font-size: 1.1rem;
      }

      .nav-toggle {
        display: block;
      }

      .nav-toggle.active span:nth-child(1) {
        transform: rotate(45deg) translate(5px, 5px);
      }

      .nav-toggle.active span:nth-child(2) {
        opacity: 0;
      }

      .nav-toggle.active span:nth-child(3) {
        transform: rotate(-45deg) translate(5px, -5px);
      }

      .hero-content {
        grid-template-columns: 1fr;
        padding: 40px 24px 60px;
        gap: 40px;
      }

      .hero-image-card {
        max-height: 360px;
        width: 100%;
      }

      .hero h1 {
        font-size: 2.4rem;
      }

      .hero-trust {
        gap: 28px;
      }

      .kurse-grid {
        grid-template-columns: 1fr;
        gap: 16px;
      }

      .kontakt-grid {
        grid-template-columns: 1fr;
        gap: 40px;
      }

      .kontakt-form {
        padding: 28px;
      }

      .form-row {
        grid-template-columns: 1fr;
      }

      .section {
        padding: 64px 24px;
      }

      .footer-inner {
        flex-direction: column;
        text-align: center;
      }

      .footer-links {
        flex-direction: column;
        gap: 12px;
      }
    }

    @media (max-width: 480px) {
      .hero h1 {
        font-size: 2rem;
      }

      .hero-actions {
        flex-direction: column;
      }

      .btn-primary,
      .btn-ghost {
        justify-content: center;
        width: 100%;
      }

      .hero-trust {
        flex-direction: column;
        gap: 16px;
      }
    }
  </style>
</head>

<body>

  <!-- NAV -->
  <nav class="nav" id="navbar">
    <div class="nav-inner">
      <a href="#" class="nav-logo">licht<span>blick</span></a>
      <ul class="nav-links" id="navLinks">
        <li><a href="#kurse">Kurse</a></li>
        <li><a href="#ueber">Über mich</a></li>
        <li><a href="#kontakt">Kontakt</a></li>
        <li><a href="#kontakt" class="nav-cta">Probestunde buchen</a></li>
      </ul>
      <button class="nav-toggle" id="navToggle" aria-label="Menü öffnen">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-bg">
      <img
        src="https://images.unsplash.com/photo-1545389336-cf090694435e?w=1920&q=80&auto=format"
        alt="Yoga-Praxis in friedlicher Atmosphäre"
        loading="eager"
      />
    </div>
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <div class="hero-text">
        <div class="hero-badge">Yoga & Meditation in Konz</div>
        <h1>Finde deine <em>Mitte.</em></h1>
        <p class="hero-subtitle">
          Ein Ort, an dem du durchatmen kannst. Ich begleite dich mit Yoga, Meditation und Achtsamkeit — in deinem Tempo, auf deinem Weg.
        </p>
        <div class="hero-actions">
          <a href="#kontakt" class="btn-primary">
            Probestunde buchen
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </a>
          <a href="#kurse" class="btn-ghost">Kursplan ansehen</a>
        </div>
        <div class="hero-trust">
          <div class="trust-item">
            <span class="trust-value">200+</span>
            <span class="trust-label">Mitglieder</span>
          </div>
          <div class="trust-item">
            <span class="trust-value">4.9 ★</span>
            <span class="trust-label">Google Bewertung</span>
          </div>
          <div class="trust-item">
            <span class="trust-value">Seit 2015</span>
            <span class="trust-label">in Konz</span>
          </div>
        </div>
      </div>
      <div class="hero-image-card">
        <img
          src="https://images.unsplash.com/photo-1506126613408-eca07ce68773?w=800&q=80&auto=format"
          alt="Sarah Meier bei der Yoga-Praxis"
          loading="eager"
        />
        <div class="hero-image-float">
          <div class="hero-image-float-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
          </div>
          <div class="hero-image-float-text">
            <strong>Sarah Meier</strong>
            <span>Zertifizierte Yogalehrerin seit 2015</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- KURSE -->
  <section class="section kurse" id="kurse">
    <div class="section-inner">
      <div class="section-header">
        <span class="section-tag">Kursangebot</span>
        <h2 class="section-title">Finde den Kurs, der zu dir passt</h2>
        <p class="section-desc">
          Ob sanfte Dehnung oder dynamischer Flow — jeder Kurs ist so gestaltet, dass du dich willkommen fühlst. Auch ohne Vorkenntnisse.
        </p>
      </div>
      <div class="kurse-grid">

        <div class="kurs-card">
          <div class="kurs-icon">
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="#7C9885" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
          </div>
          <h3>Hatha Yoga</h3>
          <p>Klassische Haltungen, bewusste Atmung und Tiefenentspannung. Ideal für den Einstieg oder als ruhiger Ausgleich im Alltag.</p>
          <div class="kurs-meta">
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              Mo & Mi, 9:00 – 10:15
            </div>
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              75 Minuten
            </div>
          </div>
          <span class="kurs-level level-beginner">Einsteiger</span>
        </div>

        <div class="kurs-card">
          <div class="kurs-icon">
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="#7C9885" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
          </div>
          <h3>Vinyasa Flow</h3>
          <p>Fließende Bewegungen im Atemrhythmus. Kräftigend, dynamisch und meditativ zugleich — für alle, die Bewegung lieben.</p>
          <div class="kurs-meta">
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              Di & Do, 18:00 – 19:15
            </div>
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              75 Minuten
            </div>
          </div>
          <span class="kurs-level level-all">Alle Level</span>
        </div>

        <div class="kurs-card">
          <div class="kurs-icon">
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="#7C9885" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>
          </div>
          <h3>Meditation</h3>
          <p>Geführte Meditationen für mehr Klarheit und Gelassenheit. Atemübungen, Body Scans und stille Sitzmeditation.</p>
          <div class="kurs-meta">
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              Fr, 7:30 – 8:15
            </div>
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              45 Minuten
            </div>
          </div>
          <span class="kurs-level level-beginner">Einsteiger</span>
        </div>

        <div class="kurs-card">
          <div class="kurs-icon">
            <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="#7C9885" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          </div>
          <h3>Yin Yoga</h3>
          <p>Langes Halten, tiefes Loslassen. Yin Yoga wirkt auf Faszien und Gelenke — perfekt am Abend oder als Ausgleich zu intensivem Training.</p>
          <div class="kurs-meta">
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
              Mi, 19:30 – 20:45
            </div>
            <div class="kurs-meta-item">
              <svg class="kurs-meta-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
              75 Minuten
            </div>
          </div>
          <span class="kurs-level level-all">Alle Level</span>
        </div>

      </div>
    </div>
  </section>

  <!-- KONTAKT -->
  <section class="section kontakt" id="kontakt">
    <div class="section-inner">
      <div class="section-header">
        <span class="section-tag">Kontakt</span>
        <h2 class="section-title">Ich freue mich auf dich</h2>
        <p class="section-desc">
          Schreib mir eine Nachricht oder ruf einfach an. Die erste Probestunde ist kostenlos und unverbindlich.
        </p>
      </div>
      <div class="kontakt-grid">

        <div class="kontakt-info">
          <h3>Lichtblick</h3>
          <p>Yoga & Meditation in Konz</p>

          <div class="kontakt-details">
            <div class="kontakt-detail">
              <div class="kontakt-detail-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
              </div>
              <div class="kontakt-detail-text">
                <strong>Adresse</strong>
                <span>Saarstraße 12<br>54329 Konz</span>
              </div>
            </div>

            <div class="kontakt-detail">
              <div class="kontakt-detail-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
              </div>
              <div class="kontakt-detail-text">
                <strong>Telefon</strong>
                <a href="tel:+496501943210">06501 — 94 32 10</a>
              </div>
            </div>

            <div class="kontakt-detail">
              <div class="kontakt-detail-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
              </div>
              <div class="kontakt-detail-text">
                <strong>E-Mail</strong>
                <a href="mailto:hallo@lichtblick-konz.de">hallo@lichtblick-konz.de</a>
              </div>
            </div>
          </div>

          <div class="kontakt-hours">
            <h4>Kurszeiten</h4>
            <div class="kontakt-hours-row">
              <span class="kontakt-hours-day">Montag & Mittwoch</span>
              <span class="kontakt-hours-time">9:00 – 10:15</span>
            </div>
            <div class="kontakt-hours-row">
              <span class="kontakt-hours-day">Dienstag & Donnerstag</span>
              <span class="kontakt-hours-time">18:00 – 19:15</span>
            </div>
            <div class="kontakt-hours-row">
              <span class="kontakt-hours-day">Mittwoch (Yin)</span>
              <span class="kontakt-hours-time">19:30 – 20:45</span>
            </div>
            <div class="kontakt-hours-row">
              <span class="kontakt-hours-day">Freitag</span>
              <span class="kontakt-hours-time">7:30 – 8:15</span>
            </div>
            <div class="kontakt-hours-row">
              <span class="kontakt-hours-day">Samstag & Sonntag</span>
              <span class="kontakt-hours-time">Geschlossen</span>
            </div>
          </div>
        </div>

        <div class="kontakt-form">
          <h3>Probestunde anfragen</h3>
          <p>Kostenlos und unverbindlich — ich melde mich innerhalb von 24 Stunden.</p>
          <form action="#" method="POST">
            <div class="form-row">
              <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" placeholder="Dein Name" required />
              </div>
              <div class="form-group">
                <label for="email">E-Mail</label>
                <input type="email" id="email" name="email" placeholder="deine@email.de" required />
              </div>
            </div>
            <div class="form-group">
              <label for="kurs">Gewünschter Kurs</label>
              <select id="kurs" name="kurs" required>
                <option value="" disabled selected>Kurs auswählen...</option>
                <option value="hatha">Hatha Yoga — Mo & Mi, 9:00</option>
                <option value="vinyasa">Vinyasa Flow — Di & Do, 18:00</option>
                <option value="meditation">Meditation — Fr, 7:30</option>
                <option value="yin">Yin Yoga — Mi, 19:30</option>
                <option value="unsicher">Ich bin mir noch unsicher</option>
              </select>
            </div>
            <div class="form-group">
              <label for="nachricht">Nachricht <span style="font-weight: 400; color: #b2bec3;">(optional)</span></label>
              <textarea id="nachricht" name="nachricht" placeholder="Erzähl mir gerne, was dich hierher führt..."></textarea>
            </div>
            <button type="submit" class="form-submit">Probestunde anfragen</button>
            <p class="form-hint">Deine Daten werden vertraulich behandelt und nicht an Dritte weitergegeben.</p>
          </form>
        </div>

      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer-inner">
      <div class="footer-logo">licht<span>blick</span></div>
      <ul class="footer-links">
        <li><a href="#kurse">Kurse</a></li>
        <li><a href="#kontakt">Kontakt</a></li>
        <li><a href="#">Impressum</a></li>
        <li><a href="#">Datenschutz</a></li>
      </ul>
      <p class="footer-copy">© 2026 Lichtblick — Yoga & Meditation in Konz. Dies ist eine Demo-Website.</p>
    </div>
  </footer>

  <script>
    // Mobile Nav Toggle
    const navToggle = document.getElementById('navToggle');
    const navLinks = document.getElementById('navLinks');

    navToggle.addEventListener('click', () => {
      navToggle.classList.toggle('active');
      navLinks.classList.toggle('open');
    });

    // Close mobile nav on link click
    navLinks.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        navToggle.classList.remove('active');
        navLinks.classList.remove('open');
      });
    });

    // Navbar scroll shadow
    const navbar = document.getElementById('navbar');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 20) {
        navbar.classList.add('scrolled');
      } else {
        navbar.classList.remove('scrolled');
      }
    });
  </script>

</body>
</html>
