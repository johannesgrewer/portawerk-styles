---
export const prerender = true;
---

<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex, nofollow" />
  <title>Rebstock — Straußwirtschaft in Longuich an der Mosel</title>
  <meta name="description" content="Rebstock — Straußwirtschaft der Familie Schommer in Longuich an der Mosel. Wein vom eigenen Weingut, hausgemachte Speisen. Geöffnet März bis Oktober." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600&family=Lora:ital,wght@0,400;0,500;0,600;1,400;1,500&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --primary: #722F37;
      --secondary: #F5EDE0;
      --accent: #8B6914;
      --background: #FBF7F0;
      --text: #2C1810;
      --primary-light: #8a3a44;
      --primary-dark: #5a252c;
      --accent-light: #a47d1a;
      --border-subtle: #e2d5c3;
      --warm-shadow: rgba(44, 24, 16, 0.08);
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: 'Lora', Georgia, serif;
      color: var(--text);
      background-color: var(--background);
      line-height: 1.7;
      font-size: 16px;
      -webkit-font-smoothing: antialiased;
    }

    /* ============ NAVIGATION ============ */
    .nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: background-color 0.4s ease, box-shadow 0.4s ease;
    }

    .nav.scrolled {
      background-color: rgba(251, 247, 240, 0.97);
      box-shadow: 0 2px 20px var(--warm-shadow);
    }

    .nav-logo {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.6rem;
      font-weight: 600;
      color: var(--secondary);
      text-decoration: none;
      letter-spacing: 0.02em;
      transition: color 0.4s ease;
    }

    .nav.scrolled .nav-logo {
      color: var(--primary);
    }

    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      font-family: 'Lora', Georgia, serif;
      font-size: 0.9rem;
      color: var(--secondary);
      text-decoration: none;
      letter-spacing: 0.04em;
      transition: color 0.3s ease;
    }

    .nav.scrolled .nav-links a {
      color: var(--text);
    }

    .nav-links a:hover {
      color: var(--accent);
    }

    .nav-toggle {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
      background: none;
      border: none;
      padding: 4px;
    }

    .nav-toggle span {
      width: 24px;
      height: 2px;
      background-color: var(--secondary);
      transition: background-color 0.4s ease;
    }

    .nav.scrolled .nav-toggle span {
      background-color: var(--text);
    }

    /* ============ HERO ============ */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      z-index: 0;
    }

    .hero-bg img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center 40%;
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        to bottom,
        rgba(44, 24, 16, 0.25) 0%,
        rgba(114, 47, 55, 0.4) 40%,
        rgba(44, 24, 16, 0.7) 100%
      );
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      text-align: center;
      max-width: 800px;
      padding: 2rem;
    }

    .hero-vine-ornament {
      display: block;
      color: var(--accent);
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 2.2rem;
      margin-bottom: 1.5rem;
      opacity: 0.7;
      letter-spacing: 0.3em;
    }

    .hero-title {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: clamp(2.8rem, 6vw, 4.8rem);
      font-weight: 400;
      font-style: italic;
      color: #fff;
      line-height: 1.15;
      margin-bottom: 1.8rem;
      letter-spacing: -0.01em;
    }

    .hero-meta {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1rem;
      color: var(--accent);
      letter-spacing: 0.18em;
      text-transform: uppercase;
      margin-bottom: 0.6rem;
      font-weight: 500;
    }

    .hero-hours {
      font-family: 'Lora', Georgia, serif;
      font-size: 0.95rem;
      color: rgba(245, 237, 224, 0.75);
      margin-bottom: 2.5rem;
      font-weight: 400;
      font-style: italic;
    }

    .hero-cta-group {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 0.9rem 2.2rem;
      border: none;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.3s ease;
      display: inline-block;
    }

    .btn-primary {
      background-color: var(--primary);
      color: #fff;
      border: 2px solid var(--primary);
    }

    .btn-primary:hover {
      background-color: var(--primary-dark);
      border-color: var(--primary-dark);
    }

    .btn-outline {
      background-color: transparent;
      color: var(--secondary);
      border: 2px solid rgba(245, 237, 224, 0.5);
    }

    .btn-outline:hover {
      background-color: rgba(245, 237, 224, 0.1);
      border-color: var(--secondary);
    }

    .hero-border-top,
    .hero-border-bottom {
      position: absolute;
      left: 2rem;
      right: 2rem;
      height: 1px;
      background: linear-gradient(to right, transparent, var(--accent), transparent);
      z-index: 2;
      opacity: 0.3;
    }

    .hero-border-top {
      top: 5rem;
    }

    .hero-border-bottom {
      bottom: 3.5rem;
    }

    .hero-scroll-hint {
      position: absolute;
      bottom: 1.2rem;
      left: 50%;
      transform: translateX(-50%);
      z-index: 2;
      color: rgba(245, 237, 224, 0.4);
      font-size: 0.72rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      font-family: 'Cormorant Garamond', Georgia, serif;
      animation: scrollBounce 2s ease-in-out infinite;
    }

    @keyframes scrollBounce {
      0%, 100% { transform: translateX(-50%) translateY(0); }
      50% { transform: translateX(-50%) translateY(6px); }
    }

    /* ============ SECTIONS GENERAL ============ */
    .section {
      padding: 6rem 2rem;
    }

    .section-header {
      text-align: center;
      margin-bottom: 4rem;
    }

    .section-label {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 0.85rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 0.8rem;
      font-weight: 600;
    }

    .section-title {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: clamp(2rem, 4vw, 3rem);
      font-weight: 500;
      color: var(--primary);
      line-height: 1.2;
      margin-bottom: 1rem;
    }

    .section-divider {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      margin-top: 1.2rem;
      color: var(--accent);
      opacity: 0.5;
      font-size: 0.9rem;
    }

    .section-divider::before,
    .section-divider::after {
      content: '';
      width: 60px;
      height: 1px;
      background-color: var(--accent);
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
    }

    /* ============ KARTE (WEINE + SPEISEN) ============ */
    .karte {
      background-color: var(--secondary);
    }

    .karte-columns {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
    }

    .karte-column-title {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.6rem;
      font-weight: 600;
      color: var(--primary);
      margin-bottom: 0.3rem;
      letter-spacing: 0.02em;
    }

    .karte-column-subtitle {
      font-family: 'Lora', Georgia, serif;
      font-size: 0.82rem;
      font-style: italic;
      color: var(--accent);
      margin-bottom: 2rem;
    }

    .karte-item {
      padding: 1.2rem 0;
      border-bottom: 1px solid var(--border-subtle);
    }

    .karte-item:last-child {
      border-bottom: none;
    }

    .karte-item-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 1rem;
      margin-bottom: 0.2rem;
    }

    .karte-item-name {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--text);
      line-height: 1.3;
    }

    .karte-item-price {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--primary);
      white-space: nowrap;
      flex-shrink: 0;
    }

    .karte-item-grape {
      font-family: 'Lora', Georgia, serif;
      font-size: 0.82rem;
      font-style: italic;
      color: var(--accent);
      margin-bottom: 0.3rem;
    }

    .karte-item-desc {
      font-family: 'Lora', Georgia, serif;
      font-size: 0.88rem;
      color: var(--text);
      opacity: 0.7;
      line-height: 1.6;
    }

    .karte-item-badge {
      display: inline-block;
      background-color: var(--primary);
      color: #fff;
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 0.65rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 0.15rem 0.5rem;
      font-weight: 600;
      margin-left: 0.5rem;
      vertical-align: middle;
      position: relative;
      top: -1px;
    }

    .karte-note {
      text-align: center;
      margin-top: 3.5rem;
      font-style: italic;
      font-size: 0.92rem;
      color: var(--text);
      opacity: 0.6;
      max-width: 500px;
      margin-left: auto;
      margin-right: auto;
      line-height: 1.7;
    }

    /* ============ FAMILIE ============ */
    .familie {
      background-color: var(--background);
    }

    .familie-content {
      display: grid;
      grid-template-columns: 1fr 1.2fr;
      gap: 4rem;
      align-items: start;
    }

    .familie-image-wrapper {
      position: relative;
    }

    .familie-image-wrapper img {
      width: 100%;
      height: auto;
      display: block;
      filter: sepia(0.15) saturate(0.9);
    }

    .familie-image-border {
      position: absolute;
      inset: -8px;
      border: 1px solid var(--accent);
      opacity: 0.3;
      pointer-events: none;
    }

    .familie-story {
      padding-top: 0.5rem;
    }

    .timeline {
      margin-top: 2.5rem;
      margin-bottom: 2.5rem;
      position: relative;
      padding-left: 2rem;
    }

    .timeline::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0.4rem;
      bottom: 0.4rem;
      width: 1px;
      background: linear-gradient(to bottom, var(--accent), var(--border-subtle));
    }

    .timeline-entry {
      position: relative;
      margin-bottom: 1.8rem;
    }

    .timeline-entry:last-child {
      margin-bottom: 0;
    }

    .timeline-entry::before {
      content: '';
      position: absolute;
      left: -2rem;
      top: 0.55rem;
      width: 7px;
      height: 7px;
      background-color: var(--accent);
      border-radius: 50%;
      transform: translateX(-3px);
    }

    .timeline-year {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: 0.15rem;
    }

    .timeline-text {
      font-family: 'Lora', Georgia, serif;
      font-size: 0.92rem;
      color: var(--text);
      opacity: 0.8;
      line-height: 1.6;
    }

    .familie-quote {
      background-color: var(--secondary);
      padding: 2rem 2.2rem;
      border-left: 3px solid var(--accent);
      margin-top: 2.5rem;
      margin-bottom: 2.5rem;
      position: relative;
    }

    .familie-quote::before {
      content: '\201E';
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 4rem;
      color: var(--accent);
      opacity: 0.3;
      position: absolute;
      top: -0.5rem;
      left: 1rem;
      line-height: 1;
    }

    .familie-quote p {
      font-family: 'Lora', Georgia, serif;
      font-style: italic;
      font-size: 1.02rem;
      line-height: 1.8;
      color: var(--text);
      opacity: 0.85;
      position: relative;
      z-index: 1;
    }

    .familie-quote-author {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 0.85rem;
      color: var(--accent);
      font-weight: 600;
      letter-spacing: 0.08em;
      margin-top: 0.8rem;
      font-style: normal;
      display: block;
    }

    .familie-kontakt {
      margin-top: 2rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border-subtle);
    }

    .familie-kontakt h3 {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.3rem;
      font-weight: 600;
      color: var(--primary);
      margin-bottom: 1rem;
    }

    .kontakt-details {
      font-size: 0.92rem;
      line-height: 2;
      opacity: 0.8;
    }

    .kontakt-details strong {
      color: var(--primary);
      font-weight: 600;
    }

    .kontakt-details a {
      color: var(--primary);
      text-decoration: none;
      border-bottom: 1px solid transparent;
      transition: border-color 0.3s ease;
    }

    .kontakt-details a:hover {
      border-bottom-color: var(--primary);
    }

    .anfahrt-hint {
      font-family: 'Lora', Georgia, serif;
      font-style: italic;
      font-size: 0.85rem;
      color: var(--text);
      opacity: 0.55;
      margin-top: 1rem;
      line-height: 1.6;
    }

    /* ============ FOOTER ============ */
    .footer {
      background-color: var(--primary-dark);
      color: rgba(245, 237, 224, 0.7);
      padding: 3rem 2rem;
      text-align: center;
    }

    .footer-logo {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 1.8rem;
      font-weight: 600;
      color: var(--secondary);
      margin-bottom: 0.3rem;
    }

    .footer-tagline {
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-style: italic;
      font-size: 0.95rem;
      margin-bottom: 1.5rem;
      opacity: 0.6;
    }

    .footer-divider {
      width: 60px;
      height: 1px;
      background-color: var(--accent);
      margin: 1.5rem auto;
      opacity: 0.4;
    }

    .footer-legal {
      font-size: 0.8rem;
      opacity: 0.5;
    }

    .footer-legal a {
      color: rgba(245, 237, 224, 0.7);
      text-decoration: none;
    }

    .footer-legal a:hover {
      color: var(--secondary);
    }

    /* ============ DEMO BANNER ============ */
    .demo-banner {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 200;
      background-color: var(--primary-dark);
      color: var(--secondary);
      text-align: center;
      padding: 0.7rem 1rem;
      font-family: 'Cormorant Garamond', Georgia, serif;
      font-size: 0.85rem;
      letter-spacing: 0.08em;
    }

    .demo-banner a {
      color: var(--accent);
      text-decoration: none;
      font-weight: 600;
      margin-left: 0.5rem;
    }

    .demo-banner a:hover {
      text-decoration: underline;
    }

    /* ============ RESPONSIVE ============ */
    @media (max-width: 900px) {
      .karte-columns {
        grid-template-columns: 1fr;
        gap: 3rem;
      }

      .familie-content {
        grid-template-columns: 1fr;
        gap: 2.5rem;
      }

      .familie-image-wrapper {
        max-width: 500px;
      }
    }

    @media (max-width: 768px) {
      .nav-links {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        flex-direction: column;
        background-color: rgba(251, 247, 240, 0.98);
        padding: 1.5rem 2rem;
        gap: 1rem;
        box-shadow: 0 4px 20px var(--warm-shadow);
      }

      .nav-links.open {
        display: flex;
      }

      .nav-links a {
        color: var(--text);
        font-size: 1rem;
      }

      .nav-toggle {
        display: flex;
      }

      .section {
        padding: 4rem 1.5rem;
      }

      .hero-cta-group {
        flex-direction: column;
        align-items: center;
      }

      .btn {
        width: 100%;
        max-width: 280px;
        text-align: center;
      }

      .hero-border-top,
      .hero-border-bottom {
        left: 1rem;
        right: 1rem;
      }

      .familie-quote {
        padding: 1.5rem 1.5rem;
      }

      .timeline {
        padding-left: 1.5rem;
      }
    }
  </style>
</head>

<body>
  <!-- Navigation -->
  <nav class="nav" id="mainNav">
    <a href="#" class="nav-logo">Rebstock</a>
    <ul class="nav-links" id="navLinks">
      <li><a href="#karte">Karte</a></li>
      <li><a href="#familie">Die Familie</a></li>
    </ul>
    <button class="nav-toggle" id="navToggle" aria-label="Navigation umschalten">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <div class="hero-bg">
      <img
        src="https://images.unsplash.com/photo-1506377247377-2a5b3b417ebb?w=1920&q=80&auto=format"
        alt="Weinberge an der Mosel im warmen Abendlicht"
        loading="eager"
      />
    </div>
    <div class="hero-overlay"></div>
    <div class="hero-border-top"></div>
    <div class="hero-content">
      <span class="hero-vine-ornament">&mdash;&nbsp;&nbsp;&loz;&nbsp;&nbsp;&mdash;</span>
      <h1 class="hero-title">Wo die Mosel<br />den Wein k&uuml;sst.</h1>
      <p class="hero-meta">Strau&szlig;wirtschaft Rebstock &middot; Longuich &middot; Seit 1968</p>
      <p class="hero-hours">Ge&ouml;ffnet M&auml;rz bis Oktober &middot; Mi&ndash;So ab 16 Uhr</p>
      <div class="hero-cta-group">
        <a href="#karte" class="btn btn-primary">Weinkarte</a>
        <a href="#familie" class="btn btn-outline">Anfahrt</a>
      </div>
    </div>
    <div class="hero-border-bottom"></div>
    <div class="hero-scroll-hint">&darr;</div>
  </section>

  <!-- Saisonale Karte -->
  <section class="section karte" id="karte">
    <div class="container">
      <div class="section-header">
        <p class="section-label">Vom eigenen Weingut</p>
        <h2 class="section-title">Unsere Karte</h2>
        <div class="section-divider">&loz;</div>
      </div>

      <div class="karte-columns">
        <!-- WEINE -->
        <div>
          <h3 class="karte-column-title">Weine</h3>
          <p class="karte-column-subtitle">Alle vom eigenen Weingut &middot; Glas 0,2l</p>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Longuicher Herrenberg Riesling <span class="karte-item-badge">Hauswein</span></span>
              <span class="karte-item-price">4,50 / 18&thinsp;&euro;</span>
            </div>
            <p class="karte-item-grape">Riesling &middot; trocken</p>
            <p class="karte-item-desc">Frisch, mineralisch, Schiefernote. Gr&uuml;ner Apfel, Zitrus. Der Klassiker.</p>
          </div>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Maximiner Klosterlay Sp&auml;tlese</span>
              <span class="karte-item-price">5,50 / 22&thinsp;&euro;</span>
            </div>
            <p class="karte-item-grape">Riesling &middot; feinherb</p>
            <p class="karte-item-desc">Goldgelb, reife Pfirsiche, ein Hauch Honig. Elegant und vielschichtig.</p>
          </div>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Dornfelder</span>
              <span class="karte-item-price">4,00 / 16&thinsp;&euro;</span>
            </div>
            <p class="karte-item-grape">Dornfelder &middot; halbtrocken</p>
            <p class="karte-item-desc">Samtig, dunkle Beeren. Kr&auml;ftig &mdash; passt perfekt zur Winzerplatte.</p>
          </div>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Winzergl&uuml;hwein <span class="karte-item-badge">Saisonal</span></span>
              <span class="karte-item-price">3,50&thinsp;&euro;</span>
            </div>
            <p class="karte-item-grape">Hausmischung &middot; warm</p>
            <p class="karte-item-desc">Mit Nelken und Zimt. Nur in der k&uuml;hlen Saison.</p>
          </div>
        </div>

        <!-- SPEISEN -->
        <div>
          <h3 class="karte-column-title">Speisen</h3>
          <p class="karte-column-subtitle">Hausgemacht &middot; aus der Region</p>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Winzerplatte f&uuml;r Zwei <span class="karte-item-badge">Empfehlung</span></span>
              <span class="karte-item-price">18,50&thinsp;&euro;</span>
            </div>
            <p class="karte-item-desc">Schinken, K&auml;se, N&uuml;sse, frisches Brot vom Dorfb&auml;cker. Reichlich.</p>
          </div>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Flammkuchen Classique</span>
              <span class="karte-item-price">9,50&thinsp;&euro;</span>
            </div>
            <p class="karte-item-desc">Schmand, Speck, Zwiebeln. D&uuml;nn, knusprig, ehrlich.</p>
          </div>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Flammkuchen Ziegenk&auml;se</span>
              <span class="karte-item-price">10,50&thinsp;&euro;</span>
            </div>
            <p class="karte-item-desc">Honig, Walnuss, Rucola. Die moderne Variante.</p>
          </div>

          <div class="karte-item">
            <div class="karte-item-header">
              <span class="karte-item-name">Hausgemachter Kuchen</span>
              <span class="karte-item-price">3,80&thinsp;&euro;</span>
            </div>
            <p class="karte-item-desc">Wechselnd &mdash; frag nach! Immer frisch, immer gut.</p>
          </div>
        </div>
      </div>

      <p class="karte-note">
        Alle Weine vom eigenen Weingut.<br />
        Probieren geht &uuml;ber Studieren.
      </p>
    </div>
  </section>

  <!-- Die Familie -->
  <section class="section familie" id="familie">
    <div class="container">
      <div class="section-header">
        <p class="section-label">Unsere Geschichte</p>
        <h2 class="section-title">Drei Generationen, ein Rebstock</h2>
        <div class="section-divider">&loz;</div>
      </div>

      <div class="familie-content">
        <!-- Foto -->
        <div class="familie-image-wrapper">
          <img
            src="https://images.unsplash.com/photo-1559581458-67c84d000717?w=800&q=80&auto=format"
            alt="Weinreben im warmen Licht"
            loading="lazy"
          />
          <div class="familie-image-border"></div>
        </div>

        <!-- Story -->
        <div class="familie-story">
          <div class="timeline">
            <div class="timeline-entry">
              <p class="timeline-year">1968</p>
              <p class="timeline-text">Opa Heinrich pflanzt die ersten Reben an den Steilh&auml;ngen &uuml;ber Longuich. Riesling, was sonst.</p>
            </div>
            <div class="timeline-entry">
              <p class="timeline-year">1989</p>
              <p class="timeline-text">Vater Klaus er&ouml;ffnet die Strau&szlig;wirtschaft. Zwei Tische, ein paar Flaschen, Nachbarn als erste G&auml;ste.</p>
            </div>
            <div class="timeline-entry">
              <p class="timeline-year">2015</p>
              <p class="timeline-text">Marie und Thomas &uuml;bernehmen &mdash; mit neuen Ideen und altem Respekt. Flammkuchen kommt auf die Karte. Die Reben bleiben.</p>
            </div>
          </div>

          <div class="familie-quote">
            <p>Unser Wein w&auml;chst hier. Unser Essen kommt von hier. Und unsere G&auml;ste kommen immer wieder.</p>
            <span class="familie-quote-author">&mdash; Marie Schommer</span>
          </div>

          <div class="familie-kontakt">
            <h3>Besuchen Sie uns</h3>
            <div class="kontakt-details">
              <strong>Strau&szlig;wirtschaft Rebstock</strong><br />
              Maximinerstra&szlig;e 12, 54340 Longuich an der Mosel<br />
              Telefon: <a href="tel:+496502003456">06502 &mdash; 34 56</a><br />
              Saison: M&auml;rz bis Oktober &middot; Mi&ndash;So ab 16 Uhr
            </div>
            <p class="anfahrt-hint">B53, Ausfahrt Longuich. Der Rebstock steht am Moselufer &mdash; Sie riechen den Flammkuchen schon von der Stra&szlig;e.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="footer-logo">Rebstock</div>
    <p class="footer-tagline">Wo die Mosel den Wein k&uuml;sst.</p>
    <div class="footer-divider"></div>
    <p class="footer-legal">
      &copy; 2026 Strau&szlig;wirtschaft Rebstock &middot; Familie Schommer &middot; Longuich an der Mosel<br />
      <a href="#">Impressum</a> &middot; <a href="#">Datenschutz</a>
    </p>
  </footer>

  <!-- Demo Banner -->
  <div class="demo-banner">
    Demo-Website &mdash; Stil: Weinstube / Moselromantik
    <a href="/">Zur&uuml;ck zur &Uuml;bersicht</a>
  </div>

  <script>
    // Navbar scroll effect
    const nav = document.getElementById('mainNav');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 60) {
        nav.classList.add('scrolled');
      } else {
        nav.classList.remove('scrolled');
      }
    });

    // Mobile nav toggle
    const navToggle = document.getElementById('navToggle');
    const navLinks = document.getElementById('navLinks');
    navToggle.addEventListener('click', () => {
      navLinks.classList.toggle('open');
      nav.classList.add('scrolled');
    });

    // Close mobile nav on link click
    navLinks.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        navLinks.classList.remove('open');
      });
    });
  </script>
</body>
</html>
