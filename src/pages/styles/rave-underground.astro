---
export const prerender = true;
---
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex, nofollow" />
  <title>Keller — Underground Club | Trier</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Anton&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { overflow-x: hidden; }

    :root {
      --red:   #FF2D55;
      --white: #FFFFFF;
      --black: #000000;
    }

    body {
      background: var(--black);
      color: var(--white);
      font-family: 'Space Mono', monospace;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Strobe line effect */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background: repeating-linear-gradient(
        0deg,
        transparent,
        transparent 3px,
        rgba(255,255,255,0.01) 3px,
        rgba(255,255,255,0.01) 4px
      );
      pointer-events: none;
      z-index: 0;
    }

    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem 3rem;
      background: var(--black);
      border-bottom: 1px solid rgba(255,255,255,0.07);
    }
    .nav-logo {
      font-family: 'Anton', sans-serif;
      font-size: 1.3rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--white);
    }
    .nav-logo span { color: var(--red); }
    .nav-links {
      display: flex;
      gap: 0;
      list-style: none;
    }
    .nav-links a {
      display: block;
      padding: 0.6rem 1.25rem;
      font-size: 0.52rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.25);
      text-decoration: none;
      border-right: 1px solid rgba(255,255,255,0.06);
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--white); }
    .nav-tickets {
      font-family: 'Anton', sans-serif;
      font-size: 0.72rem;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: var(--black);
      background: var(--white);
      padding: 0.6rem 1.4rem;
      text-decoration: none;
      transition: background 0.2s;
    }
    .nav-tickets:hover { background: var(--red); color: var(--white); }

    .hero {
      min-height: 100vh;
      padding-top: 57px;
      position: relative;
      z-index: 1;
      display: flex;
      flex-direction: column;
    }

    /* Big headline section */
    .headline-section {
      flex: 1;
      display: flex;
      align-items: center;
      padding: 3rem 3.5rem;
      gap: 0;
      border-bottom: 1px solid rgba(255,255,255,0.06);
    }

    .hero-headline {
      font-family: 'Anton', sans-serif;
      font-size: clamp(5rem, 14vw, 12rem);
      line-height: 0.88;
      letter-spacing: -0.01em;
      text-transform: uppercase;
      color: var(--white);
      flex: 1;
    }
    .hero-headline .red { color: var(--red); }
    .hero-headline .outline {
      -webkit-text-stroke: 2px rgba(255,255,255,0.25);
      color: transparent;
    }

    .side-info {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      width: 280px;
      padding-left: 3rem;
      border-left: 1px solid rgba(255,255,255,0.06);
    }
    .si-label {
      font-size: 0.5rem;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.2);
      margin-bottom: 0.4rem;
    }
    .si-value {
      font-family: 'Anton', sans-serif;
      font-size: 1.1rem;
      letter-spacing: 0.06em;
      color: var(--white);
      line-height: 1.2;
    }
    .si-sub {
      font-size: 0.62rem;
      color: rgba(255,255,255,0.35);
      margin-top: 0.2rem;
      line-height: 1.5;
    }

    /* Event list section */
    .events-section {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
    }

    .event-col {
      padding: 2rem 2.5rem;
      border-right: 1px solid rgba(255,255,255,0.06);
    }
    .event-col:last-child { border-right: none; }

    .ev-date {
      font-size: 0.52rem;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--red);
      margin-bottom: 0.5rem;
    }
    .ev-name {
      font-family: 'Anton', sans-serif;
      font-size: 1.1rem;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      color: var(--white);
      line-height: 1.1;
      margin-bottom: 0.4rem;
    }
    .ev-artist {
      font-size: 0.65rem;
      color: rgba(255,255,255,0.4);
      margin-bottom: 0.75rem;
    }
    .ev-tags {
      display: flex;
      gap: 0.4rem;
      flex-wrap: wrap;
    }
    .ev-tag {
      font-size: 0.5rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 0.18rem 0.5rem;
      border: 1px solid rgba(255,255,255,0.1);
      color: rgba(255,255,255,0.3);
    }

    /* Bottom bar */
    .bottom-bar {
      display: grid;
      grid-template-columns: 1fr auto;
      align-items: center;
      padding: 1.5rem 3.5rem;
      border-top: 1px solid rgba(255,255,255,0.06);
      background: rgba(255,45,85,0.04);
    }
    .bb-text {
      font-size: 0.62rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.25);
    }
    .bb-cta {
      font-family: 'Anton', sans-serif;
      font-size: 0.75rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--black);
      background: var(--white);
      padding: 0.75rem 2rem;
      text-decoration: none;
      transition: background 0.2s;
    }
    .bb-cta:hover { background: var(--red); color: var(--white); }
    /* === Animations & Micro-interactions === */
    @keyframes scanPulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.55; }
    }
    @keyframes redGlow {
      0%, 100% { text-shadow: none; }
      50% { text-shadow: 0 0 30px rgba(255,45,85,0.55), 0 0 60px rgba(255,45,85,0.2); }
    }
    @keyframes shimmerSlide {
      0% { left: -60%; }
      100% { left: 140%; }
    }

    body::before { animation: scanPulse 5s ease-in-out infinite; }

    .hero-headline .red { animation: redGlow 3.5s ease-in-out infinite; }

    .event-col {
      border-top: 2px solid transparent;
      transition: background 0.3s, border-top-color 0.3s;
    }
    .event-col:hover {
      background: rgba(255,45,85,0.05);
      border-top-color: var(--red);
    }
    .ev-name { transition: letter-spacing 0.3s; }
    .event-col:hover .ev-name { letter-spacing: 0.08em; }

    .nav-tickets {
      position: relative;
      overflow: hidden;
    }
    .nav-tickets::after {
      content: '';
      position: absolute;
      top: 0; left: -60%;
      width: 40%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,45,85,0.25), transparent);
      animation: shimmerSlide 3s ease-in-out infinite 1s;
    }

    .ev-tag { transition: background 0.2s, color 0.2s, border-color 0.2s; }
    .ev-tag:hover {
      background: rgba(255,45,85,0.12);
      color: var(--red);
      border-color: rgba(255,45,85,0.25);
    }

    .hero-headline .outline { transition: -webkit-text-stroke-color 0.4s; }
    .hero-headline .outline:hover { -webkit-text-stroke-color: rgba(255,255,255,0.5); }

    /* Photo containers */
    .ph { position: relative; overflow: hidden; }
    .ph img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; transition: transform 0.55s ease; }
    .ph:hover img { transform: scale(1.05); }

    .section-wrap { max-width: 1600px; margin-left: auto; margin-right: auto; }

    /* Atmosphere section */
    .atmosphere {
      position: relative;
      z-index: 1;
      border-top: 1px solid rgba(255,255,255,0.06);
      padding: 4rem 3.5rem;
      background: var(--black);
    }
    .atmo-label {
      font-size: 0.5rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.2);
      margin-bottom: 2rem;
    }
    .atmo-grid {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr;
      gap: 1px;
      background: rgba(255,255,255,0.06);
    }
    .atmo-cell {
      background: var(--black);
      overflow: hidden;
    }
    .atmo-caption {
      padding: 1rem 1.25rem;
      font-family: 'Anton', sans-serif;
      font-size: 0.75rem;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.35);
    }

    /* Sound system section */
    .soundsystem {
      position: relative;
      z-index: 1;
      border-top: 1px solid rgba(255,255,255,0.06);
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 420px;
    }
    .sound-image-col {}
    .sound-content {
      padding: 4rem 3.5rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
      gap: 1.5rem;
      border-left: 1px solid rgba(255,255,255,0.06);
    }
    .sound-label {
      font-size: 0.5rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: var(--red);
    }
    .sound-headline {
      font-family: 'Anton', sans-serif;
      font-size: clamp(2rem, 4vw, 3.5rem);
      letter-spacing: 0.02em;
      text-transform: uppercase;
      color: var(--white);
      line-height: 0.95;
    }
    .sound-body {
      font-size: 0.72rem;
      line-height: 1.85;
      color: rgba(255,255,255,0.35);
      max-width: 42ch;
    }
    .sound-specs {
      display: flex;
      flex-direction: column;
      gap: 0.6rem;
    }
    .spec-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.6rem 0;
      border-bottom: 1px solid rgba(255,255,255,0.04);
      font-size: 0.62rem;
    }
    .spec-name { color: rgba(255,255,255,0.35); letter-spacing: 0.1em; text-transform: uppercase; }
    .spec-val { font-family: 'Anton', sans-serif; font-size: 0.85rem; color: var(--white); letter-spacing: 0.06em; }
  </style>
</head>
<body>
  <nav>
    <div class="nav-logo">KE<span>LL</span>ER</div>
    <ul class="nav-links">
      <li><a href="#">Events</a></li>
      <li><a href="#">Artists</a></li>
      <li><a href="#">Location</a></li>
      <li><a href="#">Booking</a></li>
    </ul>
    <a href="#" class="nav-tickets">Tickets</a>
  </nav>

  <section class="hero">
    <div class="headline-section">
      <h1 class="hero-headline">
        <span class="outline">UN</span><br>
        TEN<br>
        <span class="red">IST</span><br>
        OBEN.
      </h1>

      <div class="side-info">
        <div>
          <div class="si-label">Location</div>
          <div class="si-value">Keller B3</div>
          <div class="si-sub">Untergeschoss · Trier Hafen<br>Eingang Industriestraße 9</div>
        </div>
        <div>
          <div class="si-label">Kapazität</div>
          <div class="si-value">350 Personen</div>
          <div class="si-sub">Hauptfloor + Außenbereich</div>
        </div>
        <div>
          <div class="si-label">Türöffnung</div>
          <div class="si-value">23:00 Uhr</div>
          <div class="si-sub">Bis die Sonne aufgeht</div>
        </div>
      </div>
    </div>

    <div class="events-section">
      <div class="event-col">
        <div class="ev-date">Fr 14.03.</div>
        <div class="ev-name">Tekk Nacht Vol. 9</div>
        <div class="ev-artist">Kobayashi b2b Renate · DJ Messer</div>
        <div class="ev-tags">
          <span class="ev-tag">Techno</span>
          <span class="ev-tag">Industrial</span>
        </div>
      </div>
      <div class="event-col">
        <div class="ev-date">Sa 22.03.</div>
        <div class="ev-name">Acid Rain</div>
        <div class="ev-artist">Klaudia Kraft · Lützenkirchen</div>
        <div class="ev-tags">
          <span class="ev-tag">Acid</span>
          <span class="ev-tag">303</span>
        </div>
      </div>
      <div class="event-col">
        <div class="ev-date">Fr 04.04.</div>
        <div class="ev-name">Black Room</div>
        <div class="ev-artist">Residents Only · Keine Gäste</div>
        <div class="ev-tags">
          <span class="ev-tag">Minimal</span>
          <span class="ev-tag">Private</span>
        </div>
      </div>
    </div>

    <div class="bottom-bar">
      <div class="bb-text">Mitglied werden · Presale-Zugang · Keine Warteschlange</div>
      <a href="#" class="bb-cta">Tickets kaufen →</a>
    </div>
  </section>

  <!-- SECTION 2: Atmosphäre -->
  <section class="atmosphere">
    <div class="section-wrap">
      <div class="atmo-label">Der Keller — Atmosphäre</div>
      <div class="atmo-grid">
        <div class="atmo-cell">
          <div class="ph" style="height:340px;">
            <img src="https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=1200&h=800&q=80" alt="Club Atmosphäre" loading="lazy"/>
            <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.6) 0%,transparent 60%);pointer-events:none;"></div>
          </div>
          <div class="atmo-caption">Hauptfloor</div>
        </div>
        <div class="atmo-cell">
          <div class="ph" style="height:340px;">
            <img src="https://images.unsplash.com/photo-1541518763669-27fef04b14ea?auto=format&fit=crop&w=800&h=800&q=80" alt="Club Licht" loading="lazy"/>
            <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.6) 0%,transparent 60%);pointer-events:none;"></div>
          </div>
          <div class="atmo-caption">Lichtanlage</div>
        </div>
        <div class="atmo-cell">
          <div class="ph" style="height:340px;">
            <img src="https://images.unsplash.com/photo-1558618666-fcd25c85cd64?auto=format&fit=crop&w=800&h=800&q=80" alt="Underground Venue" loading="lazy"/>
            <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.6) 0%,transparent 60%);pointer-events:none;"></div>
          </div>
          <div class="atmo-caption">Außenbereich</div>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTION 3: Soundsystem & Location -->
  <section class="soundsystem">
    <div class="section-wrap" style="display:grid;grid-template-columns:1fr 1fr;width:100%;">
      <div class="sound-image-col">
        <div class="ph" style="height:100%;min-height:420px;">
          <img src="https://images.unsplash.com/photo-1542736667-069246bdbc6d?auto=format&fit=crop&w=1200&h=800&q=80" alt="Soundsystem" loading="lazy"/>
          <div style="position:absolute;inset:0;background:rgba(0,0,0,0.4);pointer-events:none;"></div>
        </div>
      </div>
      <div class="sound-content">
        <div class="sound-label">Soundsystem & Location</div>
        <h2 class="sound-headline">Funktion<br>ist<br>alles.</h2>
        <p class="sound-body">
          Kein Showroom. Kein VIP-Bereich. Nur ein Raum, ein System,
          eine Lautstärke. Das Funktion One läuft seit 2021 ohne Unterbrechung.
        </p>
        <div class="sound-specs">
          <div class="spec-row">
            <span class="spec-name">Anlage</span>
            <span class="spec-val">Funktion One Evo 5</span>
          </div>
          <div class="spec-row">
            <span class="spec-name">Leistung</span>
            <span class="spec-val">28.000 W RMS</span>
          </div>
          <div class="spec-row">
            <span class="spec-name">Fläche</span>
            <span class="spec-val">320 m²</span>
          </div>
          <div class="spec-row">
            <span class="spec-name">Eingang</span>
            <span class="spec-val">Industriestr. 9 · Trier</span>
          </div>
        </div>
      </div>
    </div>
  </section>
</body>
</html>
