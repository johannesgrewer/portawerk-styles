---
export const prerender = true;
---
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex, nofollow" />
  <title>Thalassa — Griechisches Restaurant & Meeresfrüchte | Trier</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;1,400&family=Source+Sans+3:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { overflow-x: hidden; }

    :root {
      --blue:   #1B4F9E;
      --navy:   #0D2240;
      --gold:   #E8A830;
      --white:  #FFFFFF;
      --bg:     #F5F8FF;
      --muted:  rgba(13,34,64,0.45);
      --rule:   rgba(27,79,158,0.1);
    }

    body {
      background: var(--bg);
      color: var(--navy);
      font-family: 'Source Sans 3', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Subtle wave at top */
    body::before {
      content: '';
      position: fixed;
      top: 0; left: 0; right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--navy), var(--blue), var(--gold), var(--blue), var(--navy));
      z-index: 200;
      pointer-events: none;
    }

    nav {
      position: fixed;
      top: 4px; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.25rem 3.5rem;
      background: rgba(245,248,255,0.97);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid var(--rule);
    }
    .nav-logo {
      font-family: 'Cormorant Garamond', serif;
      font-weight: 600;
      font-size: 1.2rem;
      letter-spacing: 0.1em;
      color: var(--blue);
    }
    .nav-logo small {
      display: block;
      font-family: 'Source Sans 3', sans-serif;
      font-size: 0.48rem;
      font-weight: 400;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: var(--muted);
      margin-top: -0.1rem;
    }
    .nav-links {
      display: flex;
      gap: 2.5rem;
      list-style: none;
    }
    .nav-links a {
      font-size: 0.6rem;
      font-weight: 400;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--muted);
      text-decoration: none;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--blue); }
    .nav-reserve {
      font-size: 0.6rem;
      font-weight: 500;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--white);
      background: var(--blue);
      padding: 0.65rem 1.5rem;
      text-decoration: none;
      border-radius: 3px;
      transition: background 0.2s;
    }
    .nav-reserve:hover { background: var(--navy); }

    .hero {
      min-height: 100vh;
      padding-top: 68px;
      display: grid;
      grid-template-columns: 1fr 1fr;
    }

    /* LEFT */
    .hero-left {
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 5rem 4rem 5rem 4.5rem;
      gap: 2.5rem;
      border-right: 1px solid var(--rule);
    }

    .eyebrow {
      display: flex;
      align-items: center;
      gap: 0.6rem;
      font-size: 0.58rem;
      font-weight: 400;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: var(--gold);
    }
    .eyebrow::before {
      content: '~';
      font-size: 1rem;
      line-height: 0;
    }

    .hero-headline {
      font-family: 'Cormorant Garamond', serif;
      font-weight: 600;
      font-size: clamp(2.8rem, 5vw, 4.5rem);
      line-height: 1.15;
      color: var(--navy);
    }
    .hero-headline em {
      font-style: italic;
      font-weight: 400;
      color: var(--blue);
    }

    .blue-rule {
      width: 3rem;
      height: 2px;
      background: linear-gradient(90deg, var(--blue), var(--gold));
    }

    .hero-body {
      font-size: 0.9rem;
      font-weight: 300;
      line-height: 1.95;
      color: var(--muted);
      max-width: 44ch;
    }

    /* Menu highlights */
    .menu-list {
      display: flex;
      flex-direction: column;
      border-top: 1px solid var(--rule);
    }
    .menu-row {
      display: grid;
      grid-template-columns: 1fr auto;
      align-items: start;
      gap: 1rem;
      padding: 0.9rem 0;
      border-bottom: 1px solid var(--rule);
    }
    .mr-info {}
    .mr-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1rem;
      font-weight: 600;
      color: var(--navy);
    }
    .mr-desc {
      font-size: 0.65rem;
      font-weight: 300;
      color: var(--muted);
      margin-top: 0.1rem;
      font-style: italic;
    }
    .mr-price {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1rem;
      font-weight: 400;
      color: var(--gold);
      white-space: nowrap;
      padding-top: 0.15rem;
    }

    .cta-row {
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }
    .cta-btn {
      font-size: 0.6rem;
      font-weight: 500;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--white);
      background: var(--blue);
      padding: 0.9rem 2rem;
      border-radius: 3px;
      text-decoration: none;
      transition: background 0.2s;
    }
    .cta-btn:hover { background: var(--navy); }
    .cta-link {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1rem;
      font-style: italic;
      color: var(--muted);
      text-decoration: none;
      border-bottom: 1px solid rgba(13,34,64,0.15);
      padding-bottom: 1px;
    }

    /* RIGHT */
    .hero-right {
      background: var(--blue);
      display: flex;
      flex-direction: column;
      padding: 4.5rem 3.5rem;
      gap: 2.5rem;
    }

    .right-label {
      font-size: 0.55rem;
      font-weight: 400;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.45);
    }

    /* Origin story */
    .origin-block {
      background: rgba(255,255,255,0.1);
      border-radius: 8px;
      padding: 1.75rem;
      border: 1px solid rgba(255,255,255,0.15);
    }
    .ob-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.2rem;
      font-weight: 600;
      color: var(--white);
      margin-bottom: 0.6rem;
    }
    .ob-text {
      font-size: 0.82rem;
      font-weight: 300;
      color: rgba(255,255,255,0.65);
      line-height: 1.8;
    }

    /* Specialties grid */
    .spec-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0.6rem;
    }
    .spec-item {
      background: rgba(255,255,255,0.08);
      border-radius: 6px;
      padding: 1rem;
      display: flex;
      align-items: flex-start;
      gap: 0.6rem;
    }
    .si-icon { font-size: 1.2rem; flex-shrink: 0; }
    .si-name {
      font-size: 0.82rem;
      font-weight: 400;
      color: rgba(255,255,255,0.8);
    }
    .si-origin {
      font-size: 0.6rem;
      font-weight: 300;
      color: rgba(255,255,255,0.4);
      margin-top: 0.1rem;
    }

    /* Hours & info */
    .info-strip {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin-top: auto;
    }
    .is-item {}
    .is-label {
      font-size: 0.5rem;
      font-weight: 400;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.3);
      margin-bottom: 0.3rem;
    }
    .is-value {
      font-family: 'Cormorant Garamond', serif;
      font-size: 0.95rem;
      font-weight: 400;
      color: var(--white);
      line-height: 1.4;
    }
    .is-value span {
      display: block;
      font-family: 'Source Sans 3', sans-serif;
      font-size: 0.65rem;
      font-weight: 300;
      color: rgba(255,255,255,0.45);
      margin-top: 0.1rem;
    }
    /* === Animations & Micro-interactions === */
    @keyframes barShimmer {
      0%   { background-position: 0% 50%; }
      50%  { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    body::before {
      background-size: 300% auto;
      animation: barShimmer 6s ease-in-out infinite;
    }

    .menu-row {
      transition: background 0.2s, transform 0.25s;
      cursor: default;
    }
    .menu-row:hover {
      background: rgba(27,79,158,0.04);
      transform: translateX(4px);
    }
    .mr-name { transition: color 0.2s; }
    .menu-row:hover .mr-name { color: var(--blue); }

    .spec-item {
      transition: background 0.2s, transform 0.2s;
      cursor: default;
    }
    .spec-item:hover {
      background: rgba(255,255,255,0.16);
      transform: translateY(-2px);
    }

    .origin-block {
      transition: border-color 0.3s, background 0.3s;
    }
    .origin-block:hover {
      background: rgba(255,255,255,0.14);
      border-color: rgba(255,255,255,0.25);
    }

    .cta-btn {
      position: relative;
      overflow: hidden;
    }
    .cta-btn::after {
      content: '';
      position: absolute;
      top: 0; left: -60%;
      width: 40%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
      transition: left 0.5s;
    }
    .cta-btn:hover::after { left: 150%; }

    .blue-rule {
      transition: width 0.4s ease-out;
    }
    .hero-left:hover .blue-rule { width: 5rem; }

    /* Photo container */
    .ph { position:relative; overflow:hidden; }
    .ph img { position:absolute;inset:0;width:100%;height:100%;object-fit:cover;transition:transform 0.55s ease; }
    .ph:hover img { transform: scale(1.05); }

    /* Gallery section */
    .gallery-section {
      background: var(--bg);
      padding: 6rem 0;
    }
    .gallery-inner {
      max-width: 1600px;
      margin-left: auto;
      margin-right: auto;
      padding: 0 4rem;
    }
    .gallery-header {
      display: flex;
      align-items: flex-end;
      justify-content: space-between;
      margin-bottom: 3rem;
    }
    .gallery-eyebrow {
      font-size: 0.55rem;
      font-weight: 400;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 0.6rem;
    }
    .gallery-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.8rem, 3vw, 2.8rem);
      font-weight: 600;
      color: var(--navy);
      line-height: 1.2;
    }
    .gallery-title em {
      font-style: italic;
      font-weight: 400;
      color: var(--blue);
    }
    .gallery-link {
      font-size: 0.6rem;
      font-weight: 400;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--muted);
      text-decoration: none;
      border-bottom: 1px solid rgba(13,34,64,0.15);
      padding-bottom: 1px;
      white-space: nowrap;
      margin-bottom: 0.4rem;
    }
    .photo-grid {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 1.25rem;
    }
    .photo-caption {
      margin-top: 0.75rem;
    }
    .pc-name {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1rem;
      font-weight: 600;
      color: var(--navy);
    }
    .pc-sub {
      font-size: 0.65rem;
      font-weight: 300;
      color: var(--muted);
      font-style: italic;
      margin-top: 0.1rem;
    }

    /* Atmosphere section */
    .atmo-section {
      background: var(--navy);
      padding: 6rem 0;
    }
    .atmo-inner {
      max-width: 1600px;
      margin-left: auto;
      margin-right: auto;
      padding: 0 4rem;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 5rem;
      align-items: center;
    }
    .atmo-text {
      display: flex;
      flex-direction: column;
      gap: 2rem;
    }
    .atmo-eyebrow {
      font-size: 0.55rem;
      font-weight: 400;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: var(--gold);
    }
    .atmo-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2rem, 3.5vw, 3rem);
      font-weight: 600;
      color: var(--white);
      line-height: 1.2;
    }
    .atmo-title em {
      font-style: italic;
      font-weight: 400;
      color: rgba(232,168,48,0.9);
    }
    .atmo-body {
      font-size: 0.9rem;
      font-weight: 300;
      line-height: 1.95;
      color: rgba(255,255,255,0.55);
      max-width: 44ch;
    }
    .atmo-detail-list {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    .atmo-row {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 0.75rem 1rem;
      background: rgba(255,255,255,0.06);
      border-radius: 6px;
      border-left: 2px solid var(--gold);
    }
    .atmo-row-icon { font-size: 1rem; }
    .atmo-row-text {
      font-size: 0.82rem;
      font-weight: 300;
      color: rgba(255,255,255,0.7);
    }
    .atmo-cta {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.6rem;
      font-weight: 500;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--white);
      background: var(--gold);
      padding: 0.9rem 2rem;
      border-radius: 3px;
      text-decoration: none;
      transition: background 0.2s;
      width: fit-content;
    }
    .atmo-cta:hover { background: #c8901a; }
    .atmo-photo-col {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    /* Large screen fix for hero */
    @media (min-width: 1600px) {
      .hero {
        max-width: 1600px;
        margin-left: auto;
        margin-right: auto;
      }
    }

  </style>
</head>
<body>
  <nav>
    <div class="nav-logo">
      Thalassa
      <small>Griechische Küche · Trier</small>
    </div>
    <ul class="nav-links">
      <li><a href="#">Speisekarte</a></li>
      <li><a href="#">Fisch des Tages</a></li>
      <li><a href="#">Geschichte</a></li>
      <li><a href="#">Reservierung</a></li>
    </ul>
    <a href="#" class="nav-reserve">Tisch reservieren</a>
  </nav>

  <section class="hero">
    <div class="hero-left">
      <div class="eyebrow">Griechisches Restaurant · Trier</div>

      <h1 class="hero-headline">
        Das Meer<br>
        auf <em>dem</em><br>
        Teller.
      </h1>

      <div class="blue-rule"></div>

      <p class="hero-body">
        Täglich frische Lieferungen aus dem Mittelmeer.
        Familienrezepte aus Thessaloniki. Und ein Tisch,
        an dem man nicht fragt, wann man aufhört.
      </p>

      <div class="menu-list">
        <div class="menu-row">
          <div class="mr-info">
            <div class="mr-name">Fisch des Tages</div>
            <div class="mr-desc">Täglich frisch, gegrillte Zubereitung</div>
          </div>
          <div class="mr-price">ab 28 €</div>
        </div>
        <div class="menu-row">
          <div class="mr-info">
            <div class="mr-name">Oktopus vom Grill</div>
            <div class="mr-desc">Kalamata-Oliven, Zitrone, Oregano</div>
          </div>
          <div class="mr-price">22 €</div>
        </div>
        <div class="menu-row">
          <div class="mr-info">
            <div class="mr-name">Moussaka (Hausart)</div>
            <div class="mr-desc">Oma Elenis Originalrezept seit 1978</div>
          </div>
          <div class="mr-price">18 €</div>
        </div>
      </div>

      <div class="cta-row">
        <a href="#" class="cta-btn">Tisch reservieren →</a>
        <a href="#" class="cta-link">Vollständige Karte</a>
      </div>
    </div>

    <div class="hero-right">
      <div class="right-label">Unsere Geschichte</div>

      <div class="origin-block">
        <div class="ob-title">Seit 1988 in Trier</div>
        <div class="ob-text">
          Stavros Papadakis kam 1988 aus Thessaloniki nach Trier —
          und brachte die Küche seiner Mutter mit. Heute kocht
          seine Tochter Dimitra weiter, was die Familie ausmacht:
          ehrliche Zutaten, viel Liebe, kein Kompromiss.
        </div>
      </div>

      <div class="spec-grid">
        <div class="spec-item">
          <span class="si-icon">🐟</span>
          <div>
            <div class="si-name">Frischer Fisch</div>
            <div class="si-origin">Tägl. aus Thessaloniki</div>
          </div>
        </div>
        <div class="spec-item">
          <span class="si-icon">🫒</span>
          <div>
            <div class="si-name">Olivenöl</div>
            <div class="si-origin">Kreta, kaltgepresst</div>
          </div>
        </div>
        <div class="spec-item">
          <span class="si-icon">🍷</span>
          <div>
            <div class="si-name">Griechischer Wein</div>
            <div class="si-origin">Retsina, Assyrtiko</div>
          </div>
        </div>
        <div class="spec-item">
          <span class="si-icon">🧀</span>
          <div>
            <div class="si-name">Echter Feta</div>
            <div class="si-origin">g.U. Griechenland</div>
          </div>
        </div>
      </div>

      <div class="info-strip">
        <div class="is-item">
          <div class="is-label">Öffnungszeiten</div>
          <div class="is-value">
            Di – So
            <span>12:00 – 14:30 · 17:30 – 22:30</span>
          </div>
        </div>
        <div class="is-item">
          <div class="is-label">Adresse</div>
          <div class="is-value">
            Fleischstraße 38
            <span>54290 Trier · Fußgängerzone</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- GALLERY SECTION -->
  <section class="gallery-section">
    <div class="gallery-inner">
      <div class="gallery-header">
        <div>
          <div class="gallery-eyebrow">Aus unserer Küche</div>
          <h2 class="gallery-title">Fisch, Feuer &<br><em>Mittelmeer-Luft.</em></h2>
        </div>
        <a href="#" class="gallery-link">Vollständige Speisekarte →</a>
      </div>
      <div class="photo-grid">
        <div>
          <div class="ph" style="height:320px;border-radius:6px;">
            <img src="https://images.unsplash.com/photo-1559839734-2b71ea197ec2?auto=format&fit=crop&w=1200&h=800&q=80" alt="Gegrillter Fisch mit Meeresfrüchten" loading="lazy" />
            <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.45) 0%,transparent 60%);pointer-events:none;"></div>
          </div>
          <div class="photo-caption">
            <div class="pc-name">Fisch des Tages</div>
            <div class="pc-sub">Täglich frisch aus dem Mittelmeer</div>
          </div>
        </div>
        <div>
          <div class="ph" style="height:320px;border-radius:6px;">
            <img src="https://images.unsplash.com/photo-1504893524553-b855bce32c67?auto=format&fit=crop&w=1200&h=800&q=80" alt="Griechische Meeresküste" loading="lazy" />
            <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.45) 0%,transparent 60%);pointer-events:none;"></div>
          </div>
          <div class="photo-caption">
            <div class="pc-name">Oktopus vom Grill</div>
            <div class="pc-sub">Kalamata-Oliven, Zitrone, Oregano</div>
          </div>
        </div>
        <div>
          <div class="ph" style="height:320px;border-radius:6px;">
            <img src="https://images.unsplash.com/photo-1540420773420-3366772f4999?auto=format&fit=crop&w=1200&h=800&q=80" alt="Mediterrane Speisen und Wein" loading="lazy" />
            <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.45) 0%,transparent 60%);pointer-events:none;"></div>
          </div>
          <div class="photo-caption">
            <div class="pc-name">Moussaka & Mezze</div>
            <div class="pc-sub">Hausrezepte seit 1988</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ATMOSPHERE SECTION -->
  <section class="atmo-section">
    <div class="atmo-inner">
      <div class="atmo-text">
        <div class="atmo-eyebrow">~ Reservierung & Atmosphäre</div>
        <h2 class="atmo-title">Ein Abend,<br>den man<br><em>nicht vergisst.</em></h2>
        <p class="atmo-body">
          Thalassa steht für mehr als Essen — es ist der Moment,
          wenn Wein eingeschenkt wird, das Brot noch warm ist
          und die Zeit keine Rolle mehr spielt.
        </p>
        <div class="atmo-detail-list">
          <div class="atmo-row">
            <span class="atmo-row-icon">🪑</span>
            <span class="atmo-row-text">45 Sitzplätze innen · 20 auf der Terrasse</span>
          </div>
          <div class="atmo-row">
            <span class="atmo-row-icon">🎶</span>
            <span class="atmo-row-text">Live-Musik freitags ab 20 Uhr</span>
          </div>
          <div class="atmo-row">
            <span class="atmo-row-icon">🚗</span>
            <span class="atmo-row-text">Parkhaus Porta Nigra · 3 Min. zu Fuß</span>
          </div>
        </div>
        <a href="#" class="atmo-cta">Tisch reservieren →</a>
      </div>
      <div class="atmo-photo-col">
        <div class="ph" style="height:260px;border-radius:6px;">
          <img src="https://images.unsplash.com/photo-1414235077428-338989a2e8c0?auto=format&fit=crop&w=1200&h=800&q=80" alt="Restaurantatmosphäre mediterran" loading="lazy" />
          <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.35) 0%,transparent 60%);pointer-events:none;"></div>
        </div>
        <div class="ph" style="height:180px;border-radius:6px;">
          <img src="https://images.unsplash.com/photo-1551836022-d5d88e9218df?auto=format&fit=crop&w=1200&h=600&q=80" alt="Griechischer Wein und Abendstimmung" loading="lazy" />
          <div style="position:absolute;inset:0;background:linear-gradient(to top,rgba(0,0,0,0.35) 0%,transparent 60%);pointer-events:none;"></div>
        </div>
      </div>
    </div>
  </section>

</body>
</html>
